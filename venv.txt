# venv inherits the Python version it was created with
python -m venv env
python3.9 -m venv env  OR  python -m venv --python=python3.9 env
source env/bin/activate
pip install package_name
deactivate
pip freeze > requirements.txt
rm -rf env

In .gitignore file
# Virtual environment
env/
venv/
myenv/
my_env/
*.env/
